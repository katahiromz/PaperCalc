{
  "name": "papercalc",
  "version": "0.0.0",
  "description": "Written calculation in JavaScript",
  "scripts": {
    "build": "tsc && node scripts/copy-assets.js",
    "watch": "tsc -w",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
